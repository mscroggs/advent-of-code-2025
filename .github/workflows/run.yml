name: 🧪

on:
  push:
    branches: ["**"]

jobs:
  run-tests-rust:
    name: Run Rust tests
    runs-on: ubuntu-latest
    steps:
      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
      - uses: actions/checkout@v4
      - name: Cargo test
        run: cargo test
